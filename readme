📖 NLP RAG 全端系統
這是一個簡易的全端專案，包含：

前端：FastAPI + Jinja2 (網頁介面)

後端：Python (AI Agent + RAG)

資料庫：MySQL (使用 Docker 管理)

AI：LangChain + OpenAI Embeddings + FAISS
------------------------------------------------

🚀 環境需求
在開始之前，請先安裝以下工具：

Python 3.9+

Docker Desktop（Windows/Mac）

VSCode（建議使用）

pip 套件管理工具
------------------------------------------------
📂 專案結構
nlp_rag_project/
├── docker-compose.yml        # Docker MySQL 設定
├── .env                      # 環境變數
├── app/
│   └── services/
│       ├── database.py           # MySQL 封裝
│       ├── rag_agent.py          # RAG Agent
│       └── qa_service.py         # 問答服務
│   └── templates/
│       └── index.html            # 前端頁面
│   └── static/
│       └── script.js
│       └── style.css
│   └── main.py
│   └── init_db.py
├── sql/
│   ├── init_db.sql
├── faiss_index/
│   ├── default
│   ├── finance           
│   ├── legal          
│   ├── medical 
│   └── tech         
├── requirements.txt
├── agent_corpora.json
├── build_index.py
├── docker-compose.yml
├── embeddings_factory.py
├── export_corpora_to_json.py
├── readme.md                   
└── run_git_command.py

------------------------------------------------

🔧 常見問題
Docker 無法啟動：請確認 Docker Desktop 已安裝並啟動。

MySQL 連線失敗：檢查 .env 中的帳號密碼是否與 docker-compose.yml 一致。

OpenAI API 錯誤：請確認你有正確的 API Key，並且已設定環境變數。
-------------------------------------------------

## 安全性注意事項
- **API 金鑰管理**：請將金鑰存放於 `.env` 檔案或環境變數中，避免直接寫入程式碼。
- `.gitignore` 應排除 `.env` 檔案，避免金鑰洩漏。


🧪 使用方式
啟動 Docker MySQL

啟動 FastAPI (uvicorn main:app --reload)

打開瀏覽器輸入 http://127.0.0.1:8000

在網頁輸入問題，AI Agent 會檢索語料並回答

問答紀錄會存入 MySQL
-------------------------------------------------

